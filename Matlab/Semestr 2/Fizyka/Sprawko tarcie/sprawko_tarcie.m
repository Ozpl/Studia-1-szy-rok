M = 96;
m = 195;
alfa = 14;
pier2a = M/(m*cosd(alfa)) - tand(alfa)
alfa=43;
pier2b= tand(alfa)-M/(m*cosd(alfa))

tand(17.5)
M = 60;
m = 169;

alfa = 4.2;
drug2a =  M/(m*cosd(alfa)) - tand(alfa)
alfa = 39;
drug2b = tand(alfa)-M/(m*cosd(alfa))

tand(20.2)

M = 143;
m = 283;

alfa = 11;
trze2a =  M/(m*cosd(alfa)) - tand(alfa)
alfa = 45;
trze2b = tand(alfa)-M/(m*cosd(alfa))

a = 17;
b = 16;
c = 16.5;
sr = (a+b+c)/3;

niepewnoscpier1 = sqrt( ((a-sr)^2 + (b-sr)^2 + (c-sr)^2) / 3*2)

a = 17;
b = 17.5;
c = 18;
sr = (a+b+c)/3;

niepewnoscdrug1 = sqrt( ((a-sr)^2 + (b-sr)^2 + (c-sr)^2) / 3*2)

a = 19.8;
b = 20.2;
c = 20.4;
sr = (a+b+c)/3;

niepewnosctrze1 = sqrt( ((a-sr)^2 + (b-sr)^2 + (c-sr)^2) / 3*2)

a = 4;
b = 4.2;
c = 4.4;
sr = (a+b+c)/3;

niepewnoscdru2a = sqrt( ((a-sr)^2 + (b-sr)^2 + (c-sr)^2) / 3*2)

a = 43;
b = 42.5;
c = 43.5;
sr = (a+b+c)/3;

niepewnoscpier2b = sqrt( ((a-sr)^2 + (b-sr)^2 + (c-sr)^2) / 3*2)

a = 45;
b = 44;
c = 46;
sr = (a+b+c)/3;

niepewnosctrze2b = sqrt( ((a-sr)^2 + (b-sr)^2 + (c-sr)^2) / 3*2)